/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.sanity.io', 'images.pexels.com','media.istockphoto.com','images.unsplash.com'],
    },
  };
  
  module.exports = nextConfig;